import { createFileRoute } from "@tanstack/react-router"; import { useState } from "react"; import { MapPin, Plus, Search } from "lucide-react"; import { AppShell, PageTitle, panel } from "@/components/campushub/app-shell"; import { Button } from "@/components/ui/button"; import { Input } from "@/components/ui/input"; import { lostItems } from "@/lib/campus-data";
export const Route=createFileRoute("/_authenticated/lost-found")({head:()=>({meta:[{title:"Lost & Found — CampusHub"},{name:"description",content:"Search and report lost or found campus items."},{property:"og:title",content:"Lost & Found — CampusHub"},{property:"og:description",content:"Help campus items find their way home."},{property:"og:type",content:"website"},{name:"twitter:card",content:"summary_large_image"}]}),component:LostFound});
function LostFound(){const[q,setQ]=useState("");const[filter,setFilter]=useState("All");return <AppShell><PageTitle eyebrow="Community desk" title="Lost & Found" copy="A simple place to report, search, and reconnect." action={<Button className="shrink-0 rounded-full" onClick={()=>alert("Item form ready for your details")}><Plus/> Add item</Button>}/><div className="mb-6 grid gap-3 sm:grid-cols-[1fr_auto]"><label className="relative"><Search className="absolute left-3 top-3 size-4 text-muted-foreground"/><Input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search items" className="h-10 rounded-xl bg-surface pl-10"/></label><div className="flex gap-2">{["All","Lost","Found"].map(x=><Button key={x} variant={filter===x?"default":"glass"} onClick={()=>setFilter(x)}>{x}</Button>)}</div></div><div className="grid gap-4 sm:grid-cols-2">{lostItems.filter(x=>(filter==="All"||x.status===filter)&&x.title.toLowerCase().includes(q.toLowerCase())).map(x=><article className={panel} key={x.title}><div className="flex justify-between"><span className="rounded-full bg-primary/15 px-2.5 py-1 text-xs text-lavender">{x.status}</span><span className="text-xs text-muted-foreground">{x.date}</span></div><h2 className="mt-5 text-lg font-semibold">{x.title}</h2><p className="mt-1 flex items-center gap-2 text-sm text-muted-foreground"><MapPin className="size-4"/>{x.place}</p><Button variant="glass" className="mt-5 rounded-full">View details</Button></article>)}</div></AppShell>}