import { createFileRoute } from "@tanstack/react-router"; import { useState } from "react"; import { Download, Eye, FileText, Search } from "lucide-react"; import { AppShell, PageTitle, panel } from "@/components/campushub/app-shell"; import { Button } from "@/components/ui/button"; import { Input } from "@/components/ui/input"; import { resources } from "@/lib/campus-data";
export const Route=createFileRoute("/_authenticated/resources")({head:()=>({meta:[{title:"Resources — CampusHub"},{name:"description",content:"Search student notes, papers, lab materials and placement resources."},{property:"og:title",content:"Student Resources — CampusHub"},{property:"og:description",content:"Study materials and placement resources in one place."},{property:"og:type",content:"website"},{name:"twitter:card",content:"summary_large_image"}]}),component:Resources});
function Resources(){const[q,setQ]=useState("");const[type,setType]=useState("All");const rows=resources.filter(r=>(type==="All"||r.type===type)&&r.title.toLowerCase().includes(q.toLowerCase()));return <AppShell><PageTitle eyebrow="Knowledge library" title="Resources" copy="Reliable material, organized for fast access."/><div className="mb-6 grid gap-3 sm:grid-cols-[1fr_auto]"><label className="relative"><Search className="absolute left-3 top-3 size-4 text-muted-foreground"/><Input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search resources" className="h-10 rounded-xl bg-surface pl-10"/></label><select value={type} onChange={e=>setType(e.target.value)} className="h-10 rounded-xl border border-border bg-card px-3 text-sm">{["All","Notes","Question Papers","Lab Materials","Placement Resources"].map(x=><option key={x}>{x}</option>)}</select></div><div className="grid gap-3">{rows.map(r=><article className={`${panel} grid grid-cols-[auto_minmax(0,1fr)_auto] items-center gap-4`} key={r.title}><span className="grid size-11 place-items-center rounded-xl bg-primary/15 text-lavender"><FileText className="size-5"/></span><div className="min-w-0"><h2 className="truncate font-semibold">{r.title}</h2><p className="text-xs text-muted-foreground">{r.type} · {r.meta} · {r.size}</p></div><div className="flex gap-1"><Button size="icon" variant="ghost" title="View"><Eye/></Button><Button size="icon" variant="ghost" title="Download"><Download/></Button></div></article>)}</div></AppShell>}