import { createFileRoute } from "@tanstack/react-router"; import { useState } from "react"; import { CalendarDays, MapPin } from "lucide-react"; import { AppShell, PageTitle } from "@/components/campushub/app-shell"; import { Button } from "@/components/ui/button"; import { events } from "@/lib/campus-data";
export const Route=createFileRoute("/_authenticated/events")({head:()=>({meta:[{title:"Events — CampusHub"},{name:"description",content:"Discover technical, cultural, sports and workshop events."},{property:"og:title",content:"Campus Events — CampusHub"},{property:"og:description",content:"Find and register for the latest campus events."},{property:"og:type",content:"website"},{name:"twitter:card",content:"summary_large_image"}]}),component:Events});
function Events(){const [filter,setFilter]=useState("All");return <AppShell><PageTitle eyebrow="Campus calendar" title="Events" copy="Find your next room, team, crowd, or idea."/><div className="mb-6 flex gap-2 overflow-x-auto pb-2">{["All","Technical","Cultural","Sports","Workshop"].map(x=><Button key={x} variant={filter===x?"default":"glass"} className="rounded-full" onClick={()=>setFilter(x)}>{x}</Button>)}</div><div className="grid gap-5 sm:grid-cols-2">{events.filter(e=>filter==="All"||e.category===filter).map(e=><article className="overflow-hidden rounded-2xl border border-border bg-surface" key={e.title}><img src={e.image} alt="" loading="lazy" width={1200} height={800} className="aspect-[16/8] w-full object-cover"/><div className="p-5"><div className="flex items-center justify-between gap-3"><span className="text-xs text-lavender">{e.category}</span><span className="font-mono text-xs text-muted-foreground">{e.date}</span></div><h2 className="mt-2 text-xl font-semibold">{e.title}</h2><p className="mt-3 flex items-center gap-2 text-sm text-muted-foreground"><CalendarDays className="size-4"/>{e.time}<MapPin className="ml-2 size-4"/>{e.location}</p><Button className="mt-5 w-full rounded-full" onClick={()=>alert(`Registered for ${e.title}`)}>Register</Button></div></article>)}</div></AppShell>}